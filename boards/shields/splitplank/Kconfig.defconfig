if SHIELD_SPLITPLANK_LEFT

config ZMK_KEYBOARD_NAME
    default "Plank Split"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_SPLITPLANK_LEFT || SHIELD_SPLITPLANK_RIGHT

config ZMK_SPLIT
    default y

endif
